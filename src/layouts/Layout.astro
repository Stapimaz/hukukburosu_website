---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getLangFromUrl, useTranslations, url } from '../i18n/utils';

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const metaDesc = description || (lang === 'tr' 
  ? 'NS Hukuk Bürosu & Danışmanlık - Av. Samet Ümit ve Av. Öykü Naz Karaman tarafından kurulan, Ankara merkezli kurumsal, stratejik ve güvenilir hukuki çözüm ortaklığı. İcra, Ticaret ve İş Hukuku alanlarında uzman.'
  : 'NS Law Firm & Consultancy - Based in Ankara, providing strategic, reliable, and modern corporate legal solutions founded by Atty. Samet Ümit & Atty. Öykü Naz Karaman.');
---

<!doctype html>
<html lang={lang} class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={url('/favicon.svg')} />
    <meta name="generator" content={Astro.generator} />
    
    <link rel="canonical" href={Astro.url.href} />

    <title>{title} | {t('hero.title')}</title>
    <meta name="description" content={metaDesc} />

    <!-- OpenGraph / Social Media Meta -->
    <meta property="og:title" content={`${title} | ${t('hero.title')}`} />
    <meta property="og:description" content={metaDesc} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:image" content={url('/images/hero-bg.png')} />
  </head>
  <body class="min-h-screen flex flex-col bg-cream-100 text-navy-900 transition-colors duration-300">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
